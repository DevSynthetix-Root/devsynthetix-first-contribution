name: Auto Assign Issue

on:
  issue_comment:
    types: [created]

permissions:
  issues: write

jobs:
  assign-contributor:
    runs-on: ubuntu-latest
    steps:
      - name: Assign User on Comment
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const commentBody = context.payload.comment.body.toLowerCase();
            const username = context.payload.comment.user.login;
            const issueNumber = context.issue.number;

            // Smart Keywords: assign 
            const triggers = ['assign', 'work', 'claim', 'reserved'];

            // Check logic: Kya comment mein upar wala koi shabd hai?
            if (triggers.some(keyword => commentBody.includes(keyword))) {
              
              // 1. Issue Assign karo
              await github.rest.issues.addAssignees({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                assignees: [username]
              });

              // 2. Reply comment karo
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: `Hey @${username}, you have been assigned! 🚀\n\n1. Fork the repo.\n2. Add your name in \`contributors/README.md\`.\n3. Create a PR and mention **Closes #${issueNumber}** in the description.`
              });
            }
            
